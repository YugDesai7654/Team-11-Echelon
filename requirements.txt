# Core dependencies
streamlit>=1.28.0
torch>=2.0.0
torchvision>=0.15.0
transformers>=4.35.0
python-dotenv>=1.0.0
Pillow>=10.0.0
numpy>=1.24.0

# CLIP dependencies
ftfy>=6.1.0
regex>=2023.0.0

# Gemini & Search dependencies
google-generativeai
opencv-python
tqdm
tenacity
duckduckgo-search
sentencepiece
protobuf

# Out-of-context detection (Reverse Image Search)
google-search-results  # SerpApi client
python-dateutil  # Date parsing

# AI Image Detection (HuggingFace)
accelerate  # Required for SigLIP model

# Chat with Evidence feature
groq  # Llama3 for chat interface
